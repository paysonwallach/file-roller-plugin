shared_module (
  meson.project_name(),
  'Plugin.vala',
  dependencies: [
    gee_dep,
    glib_dep,
    gobject_dep,
    granite_dep,
    gtk_dep,
    pantheon_files_core_dep,
    posix_dep
  ],
  install: true,
  install_dir: files_plugin_dir
)
